name: Wake Up Hugging Face
on:
  schedule:
    - cron: '*/40 * * * *' # প্রতি ৪০ মিনিট পর পর রান করবে
  workflow_dispatch: 

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send Ping Request
        run: curl -I https://nirjhar8-testing.hf.space
